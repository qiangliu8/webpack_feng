import {
} from 'store/actions/action-types'

const initialState = {
	demoHome: 'DEMO_HOME'
}

const home = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state
	}
}

export default home